function [pvec, pstruct] = uhgf_binary_pu_tbt_transp(r, ptrans)
% Parameter transformation for uHGF binary variant with trial-by-trial perceptual uncertainty.
% Delegates to hgf_binary_pu_tbt_transp (identical transformation).

[pvec, pstruct] = hgf_binary_pu_tbt_transp(r, ptrans);

end
