function [pvec, pstruct] = uhgf_binary_transp(r, ptrans)
% Parameter transformation for uHGF binary variant.
% Delegates to hgf_binary_transp (identical transformation).

[pvec, pstruct] = hgf_binary_transp(r, ptrans);

end
